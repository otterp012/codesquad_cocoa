
const { PI } = Math;

// exports.area = (r) => PI * r ** 2;
// exports.circumference = (r) => PI * r;

const circle = {
    area: (r) => PI * r ** 2,
    circumference: (r) => PI * r
};



module.exports = circle;